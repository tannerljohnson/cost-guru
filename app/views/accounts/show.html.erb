<h1><%= @account.name %> Account Overview</h1>
<div>
    <% if @account.is_connected? %>
        ✅ Connected
    <% else %>
        ❌ Not connected
    <% end %>
</div>
<h2>Cost Overview</h2>
<div>
    Current spend this month: <%= number_to_currency(@cost_summary[:this_month_current]) %>
</div>
<div>
    Expected month-end costs: <%= number_to_currency(@cost_summary[:this_month_forecast]) %>
</div>
<div>
    How far are we through the month? <%= TimeHelpers.percentage_of_month_passed %>%
</div>
<br/>
<div>
    <%= link_to "CSP Analyses for #{@account.name}", account_analyses_path(@account) %>
</div>
<div>
    <%= link_to "Back to Accounts", accounts_path %>
</div>